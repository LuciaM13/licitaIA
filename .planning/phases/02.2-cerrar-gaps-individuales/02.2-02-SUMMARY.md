---
phase: 02.2-cerrar-gaps-individuales
plan: "02"
subsystem: notebook-validacion-bc3
tags: [notebook, mappings, partida-selector, capitulo-emasesa, gap-bc3, santa-gema, arsenal, argentina]

# Dependency graph
requires:
  - phase: 02.2-cerrar-gaps-individuales
    provides: pct_obra_accesoria como palanca residual (Plan 02.2-01)
  - phase: 02-validacion-catalogo
    provides: invariante 141 casos certificados Excel EMASESA
provides:
  - PartidaSelector con campo capitulo_emasesa para filtrado canónico EMASESA
  - seleccionar_partidas con cadena de filtros canonical-first (capitulo_emasesa → capitulo BC3-interno → descripcion_substr → codigo_exacto)
  - MAPPINGS Santa Gema corregido (ABA usa cap canónico 01, SAN usa cap canónico 02 con agregar=True)
  - MAPPINGS Arsenal corregido (m2_calzada_aba usa cap canónico 03, elimina capitulo="04" erróneo)
  - MAPPINGS Argentina corregido (m2_calzada_aba usa cap canónico 03, elimina capitulo="04" erróneo)
affects: [02.2-03, 02.2-04, validacion-bc3-vs-licitaia]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Canonical-first filter chain: capítulo EMASESA canónico (heredado de cabecera de texto) precede al capítulo BC3-interno para separar redes ABA/SAN que comparten capítulo BC3"
    - "agregar=True con filtro canónico en obras ABA+SAN: separa medios antes de sumar para evitar capturar la red equivocada"

key-files:
  created: []
  modified:
    - notebook/_obras_individuales_helpers.py

key-decisions:
  - "capitulo_emasesa filter va FIRST en la cadena: en obras ABA+SAN como Santa Gema, ambas zanjas comparten capítulo BC3-interno '02', solo el canónico EMASESA ('01' vs '02') las separa"
  - "Fixes 1+2 (Santa Gema ABA y SAN) son atómicos: aplicar solo Fix 2 sin Fix 1 sumaría ambas redes (~450 m en lugar de ~247 m)"
  - "Fixes 3+4 (Arsenal y Argentina calzada) eliminan capitulo='04' erróneo (no presente en sus BC3) sustituyéndolo por capitulo_emasesa='03' (PV ABA canónico)"
  - "Stale comment del selector SAN reemplazado por nota explicativa del cambio (era 13m de 247m reales)"

patterns-established:
  - "Selector cascade pattern: filtros opcionales en orden semántico (canónico → BC3-interno → texto → código) con cortocircuito por presencia"
  - "Inline migration comment: documentar el bug previo en la línea para trazabilidad post-fix"

requirements-completed: [D-02, D-03]

# Metrics
duration: 8min
completed: 2026-05-05
---

# Phase 02.2 Plan 02: Fix selector infrastructure y MAPPINGS bugs Summary

**Añadido `capitulo_emasesa` a `PartidaSelector` y prepuesto su filtro en `seleccionar_partidas`; corregidos 4 bugs de MAPPINGS (Santa Gema ABA/SAN, Arsenal calzada, Argentina calzada) que causaban subestimación sistemática del +25% al +44% en el gap BC3 vs LicitaIA.**

## Tareas ejecutadas

### Task 1 — Extender PartidaSelector y seleccionar_partidas

**Cambio A — `PartidaSelector` (líneas 66–79):** Nuevo campo `capitulo_emasesa: Optional[str] = None` insertado entre `capitulo` y `agregar`. Docstring actualizado para documentar la cadena de filtros canonical-first.

**Cambio B — `seleccionar_partidas` (líneas 251–268):** Filtro canónico EMASESA prepuesto como primer filtro de la cadena (antes del filtro `capitulo` BC3-interno). Cadena resultante: `capitulo_emasesa → capitulo → descripcion_substr → codigo_exacto`.

**Verificación:**
- AST OK
- Línea 259 (`if selector.capitulo_emasesa is not None:`) precede a línea 261 (`if selector.capitulo is not None:`) — orden mandatorio confirmado por grep

### Task 2 — Cuatro fixes de MAPPINGS

| Obra | Selector | Antes (bug) | Después (fix) | Justificación |
|------|----------|-------------|---------------|---------------|
| Santa Gema | `longitud_zanja_aba_m` | `capitulo="02"`, `agregar=True` | `capitulo_emasesa="01"`, `agregar=True` | OC ABA canónico EMASESA (no BC3-interno '02' que también captura SAN) |
| Santa Gema | `longitud_zanja_san_m` | `capitulo="02"`, `agregar=False` | `capitulo_emasesa="02"`, `agregar=True` | OC SAN canónico; antes capturaba 13 m de 247 m reales |
| Arsenal | `m2_calzada_aba` | `capitulo="04"`, `agregar=True` | `capitulo_emasesa="03"`, `agregar=True` | PV ABA canónico; cap '04' BC3-interno no existe en Arsenal |
| Argentina | `m2_calzada_aba` | `capitulo="04"`, `agregar=True` | `capitulo_emasesa="03"`, `agregar=True` | PV ABA canónico; idéntico al fix de Arsenal |

**Atomicidad Fixes 1+2:** Aplicar Fix 2 sin Fix 1 sumaría ambas redes (~450 m). El filtro canónico separa ABA y SAN antes de la agregación.

**Comentario inline obsoleto:** El comentario `# mismo cap 02 cubre ambas zanjas; agregar pondera` del selector SAN ha sido sustituido por `# era agregar=False: capturaba solo 13m de 247m reales` (trazabilidad del fix).

## Verificación final

- `py -c "import ast; ast.parse(...)"` → **AST OK**
- `py -m pytest tests/ -q` → **297 passed, 5 skipped, 2 xfailed** (idéntico al baseline post-plan-01; sin regresiones)
- Grep `capitulo_emasesa`:
  - Línea 78: definición en PartidaSelector
  - Líneas 259–260: primer filtro en seleccionar_partidas
  - Líneas 554, 560, 709, 754: cuatro selectores corregidos

## Deviations from Plan

### [Rule 3 — Bloqueo arquitectónico] Estrategia de commits ajustada al gitignore del proyecto

- **Encontrado durante:** Tarea 1, antes del primer commit
- **Issue:** El archivo `notebook/_obras_individuales_helpers.py` (único archivo modificado por el plan) está intencionalmente bajo `.gitignore` (`notebook/` línea 82), siguiendo la convención TFG de "notebook = scratchpad de validación local, no deployable". Los commits per-task de código no son posibles sin violar dicha convención (cambiar `.gitignore` sería Rule 4 — decisión arquitectónica).
- **Fix:** Tareas 1 y 2 se ejecutaron y verificaron en working tree; el SUMMARY.md y STATE.md (bajo `.planning/`, también ignorada) se commitean con `git add -f` siguiendo el patrón establecido por el plan 02.2-01 (commit `fc1a003`). La trazabilidad de los cambios al helper queda documentada en este SUMMARY.
- **Files modified (working tree only):** `notebook/_obras_individuales_helpers.py`
- **Commit (force-added planning artifacts):** ver final commit con `--force` para SUMMARY + STATE

### Auth gates / Blockers

Ninguno.

## Self-Check

- AST parse: OK
- pytest baseline: 297 passed (sin cambio respecto a post-02.2-01)
- Grep checks (todos los acceptance criteria del PLAN): PASS
- Stale comment Santa Gema SAN: actualizado
- `agregar=False` en Santa Gema SAN: ELIMINADO (ahora `agregar=True`)
- `capitulo="04"` en Arsenal/Argentina calzada: ELIMINADO (ahora `capitulo_emasesa="03"`)

## Self-Check: PASSED
